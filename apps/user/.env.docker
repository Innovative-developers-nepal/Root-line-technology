# Rootline User Frontend — Docker environment
# NEXT_PUBLIC_* vars are passed as build args via docker-compose

NODE_ENV=production
PORT=3000

# The following are passed via docker-compose environment block:
# REVALIDATE_SECRET, GEO_LAT, GEO_LNG
